export GOPATH=$HOME
export GO15VENDOREXPERIMENT=1
# $HOME/bin is already in $PATH, so this is unnecessary unless $GOPATH is changed:
# export PATH=$GOPATH/bin:$PATH
export PATH=/usr/local/opt/go/libexec/bin:$PATH
